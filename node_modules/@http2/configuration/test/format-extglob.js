const test = require('blue-tape')
const {isExtglob} = require('../formats/isExtglob')

test('relative path', async (t) => {
  t.true(isExtglob('foo/bar'))
})

test('wildcard', async (t) => {
  t.true(isExtglob('foo/**/*'))
})

test('wrong format', async (t) => {
  t.false(isExtglob(123))
})
